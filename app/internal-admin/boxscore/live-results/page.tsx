import { PlaceholderSection } from "../PlaceholderSection";

export const metadata = { title: "Live results — Site admin" };

export default function BoxscoreLiveResultsPage() {
  return (
    <PlaceholderSection
      title="Live results"
      summary="Update in-progress show results (live match state) as on PWBS — may combine polling, realtime channels, or the same API PWBS uses today."
    />
  );
}
