import { PlaceholderSection } from "../PlaceholderSection";

export const metadata = { title: "Tag teams & stables — Site admin" };

export default function BoxscoreTagTeamsStablesPage() {
  return (
    <PlaceholderSection
      title="Tag teams & stables"
      summary="Add and edit tag teams and stables as implemented on PWBS (shared wrestler/event model)."
    />
  );
}
