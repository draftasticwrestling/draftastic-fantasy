import { PlaceholderSection } from "../PlaceholderSection";

export const metadata = { title: "Championships — Site admin" };

export default function BoxscoreChampionshipsPage() {
  return (
    <PlaceholderSection
      title="Champions & title history"
      summary="Maintain current champions and title history rows (e.g. championship_history and related PWBS flows)."
    />
  );
}
