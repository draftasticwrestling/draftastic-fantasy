import { PlaceholderSection } from "../PlaceholderSection";

export const metadata = { title: "Wrestlers (Boxscore) — Site admin" };

export default function BoxscoreWrestlersAdminPage() {
  return (
    <PlaceholderSection
      title="Wrestlers"
      summary="Add and edit wrestlers: profile details, accomplishments, and image uploads to Supabase Storage — matching PWBS admin behavior."
    />
  );
}
